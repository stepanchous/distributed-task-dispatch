syntax = "proto3";

package db;

message Scalar {
    int32 value = 1;
}

message List {
    repeated int32 values = 2;
}

message Field {
    oneof data {
        Scalar scalar = 3;
        List list = 4;
    }
}
